# Example: demonstrate check for missing required fields

# Standard
ISA: RV32IMACU
MISA_F: [0]
MISA_D: [0]
MISA_M: [0]
MSTATUS_MPP:
  distinct:
    values:
    - 0
    - 1
    - 3
    modes: saturate_s
Vendorid: 0xabc
HWALIGN_LDST: True

# USER ENVIRONMENT
USER_ENV_DIR: 'env'
USER_LINKER: 'env/link.ld'
USER_TARGET: 'SPIKE'
USER_EXECUTABLE: 'spike -c --isa=rv32imac +signature=sign '
USER_ABI: 'ilp32'
USER_SIGN: 'sign'
RISCV_PREFIX: riscv32-unknown-elf-
USER_POST_SIM: 'sign_fix.sh'
#USER_POST_SIM: "cat sign | sed 's/.\{8\}/& /g' | awk '\{print $4 " " $3 " " $2 " " $1\}' | sed 's/ /\n/g' > temp; mv temp"
#USER_POST_SIM: "cat sign|sed 's/.\\{8\\}/& /g'"

